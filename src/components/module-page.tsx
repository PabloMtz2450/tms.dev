import Link from 'next/link';
import { AppShell } from './app-shell';
import { StatusPill } from './status-pill';
export type ModuleRow={primary:string;secondary:string;metric:string;status:string;tone:'good'|'warn'|'bad'|'info'};
export function ModulePage({title,subtitle,kicker,headline,description,action='/exceptions',actionLabel='Revisar',stats,rows}:{title:string;subtitle:string;kicker:string;headline:string;description:string;action?:string;actionLabel?:string;stats:[string,string][];rows:ModuleRow[]}){return <AppShell title={title} subtitle={subtitle}><section className="module-hero panel"><div><span className="section-kicker">{kicker}</span><h2>{headline}</h2><p>{description}</p></div><Link className="primary" href={action}>{actionLabel} →</Link></section><section className="metric-strip">{stats.map(([v,l])=><article key={l}><strong>{v}</strong><span>{l}</span></article>)}</section><section className="panel"><div className="panel-head"><div><span className="section-kicker">PRIORIZADO POR IMPACTO</span><h2>Lo que importa ahora</h2></div></div><div className="simple-list">{rows.map(r=><div className="simple-row" key={r.primary}><div><strong>{r.primary}</strong><span>{r.secondary}</span></div><b>{r.metric}</b><StatusPill tone={r.tone}>{r.status}</StatusPill></div>)}</div></section></AppShell>}
