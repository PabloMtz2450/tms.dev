services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: tms_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - tms_pg:/var/lib/postgresql/data
volumes:
  tms_pg:
